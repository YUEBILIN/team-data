---
type: think
author: dengke
created: '2026-04-23T11:15:49+08:00'
index_state: indexed
---
收到，方向已对齐。针对你列出的待确认项及后续设计，明确回复如下：

1. **移动端与代码同步**：移动端 UI 重构我已经完成并提交。请拉取并合并最新主干代码，直接使用已重构的移动端与 Web 端代码，无需再单独规划移动端面板开发。
2. **Tool Use 架构**：确认 tool use 升级**必须纳入**本次需求。你提出的设计方向完全正确，后端会提供一系列标准化 API 供大模型反向调用，请严格按 tool-calling loop 推进接口改造与交互设计。
3. **AI 文档调用维度**：AI 自主读取的范围明确收敛为两个维度：
   - 第一维度：用户当前正在回复的目标文档。
   - 第二维度：该目标文档对应 index 中登记的所有关联文档。
   关于跨 thread 引用的边界，**原有的“逃生舱”方案直接放弃**。后续完全依赖 index 映射关系，不再保留手动临时追加文件的兜底入口。
4. **Index 重构与兼容性要求**：请注意，我们即将对 index 进行一轮架构重构，原有的 `Thread`、`Discuss` 等概念将升级为 `Planning`、`Executing` 等生命周期概念，index 内容结构也会同步调整。具体方向与字段定义请查阅 pivot 仓库 `AI-docs/pivot-product.md`。你的设计可基于现有数据结构先行开展，但**必须在解析层与数据模型中预留扩展点，确保能无缝兼容即将上线的新数据结构**。

请基于以上四点更新正式设计文档、接口定义及实施计划。读取预算/频控等具体数值可按你建议的初版方案先执行，后续根据实际压测数据调优。期待设计文档评审。
