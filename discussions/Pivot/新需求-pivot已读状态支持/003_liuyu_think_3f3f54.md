---
type: think
author: liuyu
created: '2026-04-28T11:41:34+08:00'
index_state: indexed
---
设计整体结构清晰、边界明确，幂等设计与乐观更新策略非常扎实，评估可进入开发阶段。建议在实现前确认以下细节并补充关键测试用例：

**1. 需确认/优化的细节**
- **校验性能**：`mark` 接口对 `matter-index.yaml` 的读取建议加内存缓存（TTL 5 分钟），避免滚动触发高频 IO；文件名匹配建议用精确 `basename` 查找替代 `endsWith`，防误判。
- **容错与编码**：明确 API 失败（404/500/断网）时的本地回滚与重试策略；确保路由层正确处理文件名 URL 编码。

**2. 建议重点补充的测试场景**
- **并发与健壮性**：模拟并发 `mark` 请求验证 `INSERT OR IGNORE` 的原子性；验证含特殊字符文件名的读写正确性。
- **前端交互容错**：Mock API 返回 500 验证 UI 数字回滚及 `markedRef` 复位；验证 `IntersectionObserver` 进出视口防抖拦截。
- **SSE 与时间同步（关键）**：
  - **跨端同步**：验证用户 A 已读后，用户 B 页面能否秒级自动刷新，以及多 Tab 间状态同步。
  - **时间权威**：验证服务端写入时间展示的正确性，确保不受客户端本地时钟修改影响。
  - **竞态处理**：验证“乐观更新插入”与“SSE 全量刷新”叠加时，UI 是否平滑无闪烁，以及断线重连后遗漏数据能否正确补齐。

补充上述细节与测试后，本期交付风险可控，建议推进开发。
