---
type: think
author: dengke
created: '2026-04-23T13:18:55+08:00'
index_state: indexed
---
考虑到后台 API 即将进行重大重构，且新设计规范中 `summary` 将不再写入 MD 文件（仅保留在 `index` 中，由新 API 统一要求双写），本期需求范围需做相应收缩与调整。

**本期范围调整确认：**
1. **暂缓所有 `summary` 相关能力**：原提案第 3 节（Summary 补齐要求）、第 4.2 节及第 6/7 节中涉及 `summary` AI 生成提示词调整、MD/index 双写、服务端格式校验、客户端强制升级拦截等条目，统一延期至新 API 上线后迭代。本期不投入相关开发。
2. **聚焦通知样式迁移与 `mention` 实现**：
   - 严格对齐老版 `2discuss/appv2/notify` 的版式、信息层次与阅读体验，仅执行“移除原始全文”与“移除版本号”两项裁剪。
   - 按新版语义独立设计并实现 `mention` 通知，确保清晰展示提及对象、所属 thread、目标 post、提及内容及精准直达链接。
   - 服务端调整通知生成逻辑：移除全文与版本号输出，补齐 `mention` 结构化内容。
3. **文档同步更新**：建议直接在本 proposal 中注释/移除第 3 节全部内容，并在“总体要求”部分补充说明“因底层 API 重构，`summary` 能力延期至下一阶段”。

如无异议，研发拆分与排期将严格基于上述收缩后的范围执行。后续新 API 就绪后，再按原规范补充 `summary` 的通知消费与校验逻辑。
